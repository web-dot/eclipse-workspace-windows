import java.util.regex.Pattern;
import java.util.regex.Matcher;
public class CMDLineStrPgms {

	public static void main(String[] args) {
		
		
		//Intermideate 
		
		//Q.16-//1) Take string inputs from arg[0], arg[1]. 
		//Find the number of occurrences of arg[1] in arg[0]
		//and print it out.
		
		/*
		String str1 = args[0];
		String str2 = args[1];
		int count = 0;
			int pos = 0;
			while((pos = str1.indexOf(str2, pos)) != -1) {
				count++;
				pos++;
			}
		System.out.println(count);
		*/
		
		////2) Take any number of strings as input from command line. 
		//Concat all of them and print them out.
		
		/*
		String str = "";
		for(int i = 0; i < args.length; i++) {
			str = str + args[i];
		}
		System.out.println(str);
		*/
		
		//3) Take a string input and reverse only the individual
		//words without using built in functionality.
		
		String str = "all the people";
		
		
		
	}

}
